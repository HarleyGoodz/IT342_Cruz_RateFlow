<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RateFlow"
        tools:targetApi="31">
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RateFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:host="localhost" android:port="3000" android:pathPattern="/reset-password" />
            </intent-filter>
        </activity>

        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".dashboard.UserDashboardActivity" />
        <activity
            android:name=".admin.AdminDashboardActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".services.AddServiceActivity"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:theme="@style/Theme.RateFlow" />

        <activity android:name=".ratings.RateServiceActivity" />
        <activity android:name=".ratings.MyRatingsActivity" />
        <activity android:name=".profile.UserProfileActivity" />
        <activity android:name=".notifications.UserNotificationActivity" />
        <activity android:name=".services.EditServiceActivity"/>
        <activity android:name=".ratings.AdminViewRatingsActivity"/>
        <activity android:name=".admin.AccessControlActivity" />
        <activity android:name=".notifications.AdminNotificationsActivity"/>
        <activity android:name=".auth.ForgotPasswordActivity" />
        <activity android:name=".auth.ResetPasswordActivity" />


        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>